<?xml version="1.0"?>
<!DOCTYPE service SYSTEM "./api-doc-source/api-doc.dtd">
<service name="装修APP 服务接口" basePath="http://192.168.1.110:8099/api/">
    <description>
        接入简单、异常监控&#x000A; 完善的机制、丰富的接口
    </description>
    <header name="token" type="string" description="登录接口返回的Token，用于保持登录状态不过期"/>

    <module name="接口说明">
        <apidescription name="接口访问">
            <![CDATA[
            本服务所有接口都已 Post 方式访问，发送的参数包括：头部（Header）,参数（Url）,实体（Body）。返回数据都是 Json格式
            <br/><br/>
            <li><b>发送-头部（Header）</b>：</li>
                <p>主要用于保持登录 的 token，作用在于当Cookie和Session失效之后，继续生成新的Cookie</p>
            <br/>
            <li><b>发送-参数（Url）</b>：</li>
                <p>目前用于发送分页参数 page（当前页数，0开始），pageSize（分页大小）</p>
                <p>例如：</p>
                <code>url?page=1&amp;pageSize=20</code>
            <br/>
            <li><b>发送-实体（Body）</b>：</li>
                <p>除了分页参数，一般其他数据都是以实体（Body）的Json格式发送，而且Json个最外层必须有 Signature 和 Param。</p>
                <pre><code>{
    Signature:"", //用于登录验证的备用字段，目前无实际用处，如果没有登录直接使用空字符串“”，如果登录，就使用用户ID
    Param:{}, //才是发送参数的主要部分，具体内容根据具体接口来定。
}</code></pre>
            <br/>
            <li><b>返回-实体（Body）</b>：Json</li>
                <p>最外层格式都有 ResultCode，Data，ErrorMsg</p>
                <pre><code>{
    ResultCode:0, //0 表示成功 否则 Data 为null ErrorMsg 可以获取错误信息
    Data:{}, //根据不同接口有不同的数据格式
    ErrorMsg:"", //字符串格式，如：密码错误
}</code></pre>
            ]]>
        </apidescription>
        <apidescription name="登录保持">
            调用登录接口之后，服务器会返回一个 Set-Cookie ，客户端必须把 Cookie 保存下来，在之后的接口中发送到服务器，才能保持登录会话。

            由于 Cookie 和 Session 会在短时间内过期，客户端也必须保存下 服务站在登录接口中返回的 Token ，之后一起发送到服务器，
            可以在 Session 过期的时候继续创建新的 Cookie。
        </apidescription>
    </module>

    <module name="全局通用">
        <api name="首页数据" requestMethod="POST" path="ios.ashx?$c=Common&amp;$m=GetHomeData$OPEN">
            <description>
                获取APP顶部幻灯片和消息数量
            </description>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                MessageCount:0//我的消息数量,
                TaskCount:0//我的任务数量,
                int NoticeCount;//未读公告,
                int ApprocalCount;//审批数量,
                Banners://顶部幻灯片[{
                String Title;// "标题",
                String ImageUrl;// "图片连接",
                }...],
                }
                }
            </response>
        </api>
        <api name="联系列表" requestMethod="POST" path="ios.ashx?$c=Common&amp;$m=GetContactsList" >
            <description>
                需要多次获取才能拿到完整的联系人树形列表
            </description>
            <param name="Id" nullable="true" description="分组Id" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//用户Id,
                string Name ;//显示名称,
                string Avatar ;//用户头像,
                string Phone ;//电话,
                bool IsGroup ;//是否是部门
                }...]
                }
            </response>
        </api>
        <api name="图片上传" requestMethod="POST" path="ios.ashx?$c=ImageFile&#38;$m=Upload">
            <description>
                infoType 的枚举
                用户 = 10,
                项目验收报告 = 20,
                施工日志 = 30,
                质量处理 = 40,
                施工图纸 = 50,
                公告 = 60,
                备忘录 = 70,
                文章 = 80,
                机构 = 90,
                签到签退 = 100,
                评价 = 110,
                审批申请 = 120,
                项目= 130,
                用户消息= 140,
            </description>
            <param name="infoType" description="如果是用户头像：用户，如果是工作日志：工作日志" type="string"/>
            <param name="infoId" description="图片所属对象的ID，如用户ID，日志ID" type="string"/>
            <body contentType="Multipart">
                图片二进制文件
            </body>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:true
                }
            </response>
        </api>
        <api name="删除图片" requestMethod="POST" path="ios.ashx?$c=ImageFile&#38;$m=Delete">
            <param name="Id" description="上传图片返回的Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:true
                }
            </response>
        </api>
    </module>

    <module name="用户相关">
        <api name="用户注册" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=Regist$OPEN">
            <form name="mobile" description="账户" type="string"/>
            <form name="password" description="密码（未加密）" type="string"/>
            <form name="verifycode" description="手机验证码" type="string"/>
            <form name="nickname" nullable="true" description="昵称" type="string"/>
            <response contentType="Json">
                <header name="token" type="string" description="登录接口返回的Token，用于保持登录状态不过期"/>
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                int Age,
                String Name,
                String Token,

                String Id ;/// 用户ID(自增),
                String UserName ;/// (原Id)	用户名,
                String NickName ;/// 昵称(没有指定随机生成),
                String Mail ;/// 邮箱,
                String QQ ;/// QQ号,
                String City ;/// 所在城市,
                String Address ;/// 详细地址,
                String Signature ;/// 签名,
                String HeadUrl ;/// 头像Url,
                String Password ;/// (原Pwd)	MD5密码,
                String PersonName; /// 姓名【住建部标准】【按该人员证件上的姓名填写】,
                bool NoticeAuthority;//发公告权限,
                int SexNum ,
                String Mobile ,
                Date Birthday;///生日,
                Organization://组织机构{
                    String Id;      //Id,
                    String Name;    //名称,
                    String Avatar;  //头像,
                    String ManagerIds ;//管理员Ids，用于判断权限,
                    Creater://创建者{
                        String Id;      //Id,
                        String Name;    //名称,
                        String Avatar;  //头像,
                        },
                    },
                    Department://部门信息{
                    String Id;      //Id,
                    String Name;    //名称,
                    }
                }
                }
            </response>
        </api>
        <api name="用户登录" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=Login$OPEN">
            <description>
                返回值中的token很重要，要保存下载，在登录之后的所有接口的头部都要加上这个参数
            </description>
            <header name="token" type="cancel"/>
            <form name="username" description="账户" type="string"/>
            <form name="password" description="密码（未加密）" type="string"/>
            <response contentType="Json">
                <header name="token" type="string" description="登录接口返回的Token，用于保持登录状态不过期"/>
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                int Age,
                String Name,
                String Token,

                String Id ;/// 用户ID(自增),
                String UserName ;/// (原Id)	用户名,
                String NickName ;/// 昵称(没有指定随机生成),
                String Mail ;/// 邮箱,
                String QQ ;/// QQ号,
                String City ;/// 所在城市,
                String Address ;/// 详细地址,
                String Signature ;/// 签名,
                String HeadUrl ;/// 头像Url,
                String Password ;/// (原Pwd)	MD5密码,
                String PersonName; /// 姓名【住建部标准】【按该人员证件上的姓名填写】,
                bool NoticeAuthority;//发公告权限,
                int SexNum ,
                String Mobile ,
                Date Birthday;///生日,
                Organization://组织机构{
                    String Id;      //Id,
                    String Name;    //名称,
                    String Avatar;  //头像,
                    Creater://创建者{
                        String Id;      //Id,
                        String Name;    //名称,
                        String Avatar;  //头像,
                    },
                },
                Department://部门信息{
                    String Id;      //Id,
                    String Name;    //名称,
                }
                }
                }
            </response>
        </api>
        <api name="注销登录" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=Logout">
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:
                }
            </response>
        </api>
        <api name="修改资料" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=Update">
            <form name="PersonName" description="昵称（姓名）" type="string" nullable="true"/>
            <form name="Address" description="地址" type="string" nullable="true"/>
            <form name="IDCard" description="身份证号" type="string" nullable="true"/>
            <form name="Mobile" description="手机号码" type="string" nullable="true"/>
            <form name="SexNum" description="性别" type="string" nullable="true"/>
            <form name="HeadUrl" description="头像地址" type="string" nullable="true"/>
            <form name="Age" description="年龄" type="int" nullable="true"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                }
                }
            </response>
        </api>
        <api name="重置手机" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=ResetMobile">
            <form name="mobile" description="手机号码" type="string"/>
            <form name="verifycode" description="手机验证码" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                }
                }
            </response>
        </api>
        <api name="重置密码" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=ResetPassword">
            <form name="oldpassword" description="原密码（未加密）" type="string"/>
            <form name="newpassword" description="新密码（未加密）" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                }
                }
            </response>
        </api>
        <api name="忘记密码" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=ForgetPassword$OPEN">
            <description>用于忘记密码，验证之后的重置</description>
            <form name="mobile" description="手机号码" type="string"/>
            <form name="password" description="新密码（未加密）" type="string"/>
            <form name="verifycode" description="手机验证码" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                }
                }
            </response>
        </api>
        <api name="注册验证" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=SendVerificationCodeBeforeRegist$OPEN">
            <description>
                用户注册时发送手机验证码，也可以更改手机号码时发送
            </description>
            <form name="mobile" description="手机号码" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{}
                }
            </response>
        </api>
        <api name="手机验证" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=SendVerificationCodeBeforeRegist$OPEN">
            <description>
                用户注册时发送手机验证码，也可以更改手机号码时发送
            </description>
            <form name="mobile" description="手机号码" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{}
                }
            </response>
        </api>
        <api name="重置验证" requestMethod="POST" path="ios.ashx?$c=User&amp;$m=SendVerificationCodeReset$OPEN">
            <description>
                重置密码时发送手机验证码
            </description>
            <form name="mobile" description="手机号码" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{}
                }
            </response>
        </api>
    </module>

    <module name="拜访相关">
        <api name="拜访列表" requestMethod="POST" path="ios.ashx?$c=Visit&amp;$m=GetList">
            <description>
                主页拜访列表
            </description>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;      //拜访Id,
                String Target;    //拜访对象,
                String Address;  //拜访地点,
                Date Time;    //拜访日期,
                Visitor://拜访人员{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="拜访详情" requestMethod="POST" path="ios.ashx?$c=Visit&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;      //拜访Id,
                String Target;    //拜访对象,
                String Address;  //拜访地点,
                String Remark;  //拜访备注,
                Date Time;    //拜访日期,
                Visitor://拜访人员{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像
                },
                Location://定位信息（可能为null）{
                double latitude,
                double longitude
                }
                }
                }
            </response>
        </api>
        <api name="添加拜访" requestMethod="POST" path="ios.ashx?$c=Visit&amp;$m=Submit">
            <form name="Target" description="拜访对象" type="string"/>
            <form name="Address" description="拜访地点" type="string"/>
            <form name="Remark" nullable="true" description="备注信息" type="string"/>
            <form name="DateTime" nullable="true" type="string" description="拜访时间（yyyy-MM-dd HH:mm）"/>
            <form name="latitude" nullable="true" type="double" description="定位信息"/>
            <form name="longitude" nullable="true" type="double" description="定位信息"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
    </module>

    <module name="公告相关">
        <api name="公告列表" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=GetMyNoticeList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                Date Time;       //发布时间,
                bool IsReaded;   //是否阅读,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="公告详情" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                Date Period;  //公告截至日期,
                Date Time;       //发布时间,
                Images://图片["url","url",...];,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="阅读公告" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=ReadById">
            <form name="Id" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                }
            </response>
        </api>
        <api name="发送对象" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=GetTargetTree" >
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//显示名称,
                string ParentId ;//父级Id
                int MemberCount ;//成员数量,
                Items:[];//树形嵌套,
                }...]
                }
            </response>
        </api>
        <api name="发布公告" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=Publish">
            <form name="Title" description="公告标题" type="string"/>
            <form name="Content" description="公告内容" type="string"/>
            <form name="TargetIds" description="接收对象Id" example="001,002,003" type="string"/>
            <form name="Period" nullable="true" type="string" description="截止时间" example="yyyy-MM-dd HH:mm"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="未读公告" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=GetMyNewNoticeList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                Date Time;       //发布时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="已读公告" requestMethod="POST" path="ios.ashx?$c=Notice&amp;$m=GetMyReadedNoticeList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                Date Time;       //发布时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
    </module>

    <module name="任务备忘">
        <api name="发布列表" requestMethod="POST" path="ios.ashx?$c=Task&amp;$m=GetMySendedTaskList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                Date Time;       //时间,
                Recipient://接收人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="接收列表" requestMethod="POST" path="ios.ashx?$c=Task&amp;$m=GetMyRecvedTaskList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="任务详情" requestMethod="POST" path="ios.ashx?$c=Task&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                Date Time;       //发布时间,
                int StartTime ;// 开始时间,
                int Period ;// 工作周期（单位为小时）,
                int Staffs ;// 需要人数,
                string Address ;// 任务地点,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Location://定位信息（可能为null）{
                double latitude,
                double longitude
                }
                }
                }
            </response>
        </api>
        <api name="发布任务" requestMethod="POST" path="ios.ashx?$c=Task&amp;$m=Publish">
            <form name="Title" description="任务名称" type="string"/>
            <form name="Content" description="任务内容" type="string"/>
            <form name="Address" description="位置信息" type="string"/>
            <form name="Period" type="int" description="工作周期（小时）" />
            <form name="Staffs" type="int" description="择委派人数" />
            <form name="StartTime" type="string" description="开始时间" example="yyyy-MM-dd HH:mm"/>
            <form name="TargetIds" description="接收对象Id" example="001,002,003" type="string"/>
            <form name="latitude" nullable="true" type="double" description="定位信息"/>
            <form name="longitude" nullable="true" type="double" description="定位信息"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="我的备忘" requestMethod="POST" path="ios.ashx?$c=Memo&amp;$m=GetMyMemoList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                Date Time;       //时间,
                }...]
                }
            </response>
        </api>
        <api name="备忘详情" requestMethod="POST" path="ios.ashx?$c=Memo&amp;$m=GetMyDemoById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                Date Period;  //公告截至日期,
                Date Time;       //发布时间,
                Images://图片["url","url",...];,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="提交备忘" requestMethod="POST" path="ios.ashx?$c=Memo&amp;$m=Submit">
            <form name="Title" description="标题" type="string"/>
            <form name="Content" description="内容" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="删除备忘" requestMethod="POST" path="ios.ashx?$c=Memo&amp;$m=DeleteMyDemoById">
            <form name="Id" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://"",
                }
            </response>
        </api>
    </module>

    <module name="日志打卡">
        <api name="发布列表" requestMethod="POST" path="ios.ashx?$c=WorkDiary&amp;$m=GetMySendedDiaryList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String CompleteWork ;// 今日完成工作,
                String UncompletedWork ;// 今日未完成工作,
                String ConcertWork ;// 协调工作,
                Date Time;       //时间,
                Recipient://接收人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="接收列表" requestMethod="POST" path="ios.ashx?$c=WorkDiary&amp;$m=GetMyRecvedDiaryList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String CompleteWork ;// 今日完成工作,
                String UncompletedWork ;// 今日未完成工作,
                String ConcertWork ;// 协调工作,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="工序日志" requestMethod="POST" path="ios.ashx?$c=WorkDiary&amp;$m=GetWorkDiaryListByProcessId">
            <form name="ProcessId" type="string" description="工序Id"/>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String CompleteWork ;// 今日完成工作,
                String UncompletedWork ;// 今日未完成工作,
                String ConcertWork ;// 协调工作,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="日志详情" requestMethod="POST" path="ios.ashx?$c=WorkDiary&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Title;    //标题,
                String CompleteWork ;// 今日完成工作,
                String UncompletedWork ;// 今日未完成工作,
                String ConcertWork ;// 协调工作,
                Images://图片["url","url",...];,
                Date Time;       //发布时间,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }
                }
            </response>
        </api>
        <api name="发布日志" requestMethod="POST" path="ios.ashx?$c=WorkDiary&amp;$m=Submit">
            <form name="Title" description="日志标题" type="string"/>
            <form name="CompleteWork" description="今日完成工作" type="string"/>
            <form name="UncompletedWork" description="今日未完成工作" type="string"/>
            <form name="ConcertWork" type="string" description="工作协调" />
            <form name="GongXuId" nullable="true" type="string" description="工序Id"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="打卡签到" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=Signin">
            <form name="Address" type="string" description="定位信息"/>
            <form name="latitude" nullable="true" type="double" description="定位信息"/>
            <form name="longitude" nullable="true" type="double" description="定位信息"/>
            <form name="Remark" nullable="true" type="string" description="备注信息"/>
            <form name="Type" nullable="true" type="int" description="类型：1日常签到2工地签到"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://签到ID
                }
            </response>
        </api>
        <api name="打卡签退" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=Signout">
            <form name="Address" type="string" description="定位信息"/>
            <form name="latitude" nullable="true" type="double" description="定位信息"/>
            <form name="longitude" nullable="true" type="double" description="定位信息"/>
            <form name="Remark" nullable="true" type="string" description="备注信息"/>
            <form name="Type" nullable="true" type="int" description="类型：1日常签到2工地签到"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://签到ID
                }
            </response>
        </api>
        <api name="打卡统计" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=StatisticsAttendanceByMonth" >
            <description>
                按月统计考勤
            </description>
            <form name="month" type="string" description="统计月份(yyyy-MM-01 00:00)" />
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                int AbsenceClock;//缺卡(次),
                int Absenteeism;//旷工（天）,
                int AttendanceDay;//出勤天数（天）,
                int NormalClock;//正常打卡（天）,
                int LeaveEarly;//早退(次),
                int BeLate;//迟到(次),
                int Evection;//出差(小时),
                int Visit;//外出(次),
                int Rest;//休息(小时),
                int vacate;//请假(小时),
                }
                }
            </response>
        </api>
        <api name="签到列表" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=GetMySignInList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;        //Id,
                String Name;      //名称,
                String Avatar;    //头像,

                Date Time ;//时间,
                String Address ;//地址,
                string TypeName ;//类型名称,
                string Department ;//部门,
                int Type;//类型：日常签到1 工地签到2

                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="签退列表" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=GetMySignOutList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;        //Id,
                String Name;      //名称,
                String Avatar;    //头像,

                Date Time ;//时间,
                String Address ;//地址,
                string TypeName ;//类型名称,
                string Department ;//部门,
                int Type;//类型：日常签到1 工地签到2

                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="打卡详情" requestMethod="POST" path="ios.ashx?$c=Signed&amp;$m=GetSignInfoById">
            <form name="Id" type="string" description="签到Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                Date Time ;// 时间,
                string Address ;// 项目地址,
                int Type ;// 类型:1日常签到2工地签到,
                int Status ;//状态:1签到2签退,
                string Remark ;// 备注,
                Signer://签到人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Project://项目（可能为null）{
                String Id;      //用户Id,
                String Name;    //显示名称,
                },
                Location://定位信息（可能为null）{
                double latitude,
                double longitude
                }
                }
                }
            </response>
        </api>
    </module>

    <module name="质量图纸">
        <api name="发布列表" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=GetMySendedQualityHandleList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Name ;// 问题标题,
                String Position ;// 隐患位置,
                String Cause ;// 隐患原因,
                String ConcertWork ;// 协调工作,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Date Time;       //时间,
                Recipient://接收人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="接收列表" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=GetMyRecvedQualityHandleList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Name ;// 问题标题,
                String Position ;// 隐患位置,
                String Cause ;// 隐患原因,
                String ConcertWork ;// 协调工作,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="工序质量" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=GetQualityHandleListByProcessId">
            <form name="ProcessId" type="string" description="工序Id"/>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Name ;// 问题标题,
                String Position ;// 隐患位置,
                String Cause ;// 隐患原因,
                String ConcertWork ;// 协调工作,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="处理详情" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Name ;// 问题标题,
                String Position ;// 隐患位置,
                String Cause ;// 隐患原因,
                String ConcertWork ;// 协调工作,
                Images://图片["url","url",...];,
                Date Time;       //发布时间,
                int Status ;// 任务处理状态:待处理 = 1;进行中 = 2;已完结 = 3,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }
                }
            </response>
        </api>
        <api name="发布处理" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=Submit">
            <form name="Name" description="标题" type="string"/>
            <form name="Position" description="隐患位置" type="string"/>
            <form name="Cause" description="隐患原因" type="string"/>
            <form name="ConcertWork" type="string" description="协调工作" />
            <form name="HandleStatus" type="int" description="处理状态" example="待处理 = 1;进行中 = 2;已完结 = 3" />
            <form name="GongXuId" nullable="true" type="string" description="工序Id"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="更新状态" requestMethod="POST" path="ios.ashx?$c=QualityHandle&amp;$m=UpdateStatusById">
            <form name="Status" type="int" description="处理状态" example="待处理 = 1,进行中 = 2,已完结 = 3"/>
            <form name="Id" type="string" description="质量处理Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="图纸列表" requestMethod="POST" path="ios.ashx?$c=ConstructDrawing&amp;$m=GetConstructDrawingList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <form name="ProjectId" nullable="true" type="string" description="项目Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Name ;   // 标题,
                String Avatar;  //头像,
                String Time ;// 时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="工序图纸" requestMethod="POST" path="ios.ashx?$c=ConstructDrawing&amp;$m=GetConstructDrawingListByProcessId">
            <form name="ProcessId" type="string" description="工序Id"/>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Name ;   // 标题,
                String Avatar;  //头像,
                String Time ;// 时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }...]
                }
            </response>
        </api>
        <api name="发布图纸" requestMethod="POST" path="ios.ashx?$c=ConstructDrawing&amp;$m=Submit">
            <form name="Name" description="名称" type="string"/>
            <form name="Description" description="描述信息" type="string"/>
            <form name="GongXuId" type="string" description="工序Id"/>
            <form name="ProjectId" type="string" description="项目Id"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="图纸详情" requestMethod="POST" path="ios.ashx?$c=ConstructDrawing&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Name ;// 名称,
                String Description ;// 描述信息,
                String Time ;// 时间,
                Images://图片["url","url",...];,
                Date Time;       //发布时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Stage://阶段（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                },
                Process://工序（可能为null）{
                String Id;      //Id,
                String Name;    //名称,
                }
                }
                }
            </response>
        </api>
    </module>

    <module name="审批相关">
        <api name="待我审批" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetMyPendingList">
            <description>
                获取带我审批的列表
            </description>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//申请名称,
                string Content ;//申请内容,
                DateTime Time ;//申请时间,
                int ApprovalType;//类型 请假 = 1;休息 = 2;出差 = 3;报销 = 4;物品领用 = 5;通用申请 = 15,
                int Status ;//状态 未审批 = 1；审批通过 = 2；审批不通过 = 3,
                Applicant://申请人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="我的申请" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetMyApplyList">
            <description>
                我发出的申请
            </description>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//申请名称,
                string Content ;//申请内容,
                DateTime Time ;//申请时间,
                int ApprovalType;//类型 请假 = 1;休息 = 2;出差 = 3;报销 = 4;物品领用 = 5;通用申请 = 15,
                int Status ;//状态 未审批 = 1；审批通过 = 2；审批不通过 = 3,
                Approver://审批人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="申请请假" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyVacate">
            <description>
                请假类别:
                事假 = 1,
                病假 = 2,
                产假 = 3,
                婚假 = 4,
                丧假 = 5,
                年假 = 6,
                公假 = 7,
            </description>
            <form name="Type" description="请假类别" type="int"/>
            <form name="StartTime" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="EndTime" type="string" description="结束时间（yyyy-MM-dd HH:mm）"/>
            <form name="Duration" type="float" description="请假时长（单位为小时）"/>
            <form name="Reason" type="string" description="请假事由（最长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="申请休息" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyRest">
            <form name="StartTime" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="EndTime" type="string" description="结束时间（yyyy-MM-dd HH:mm）"/>
            <form name="Duration" type="float" description="请假时长（单位为小时）"/>
            <form name="Reason" type="string" description="请假事由（最长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="申请出差" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyEvection">
            <form name="Address" description="出差地点（最大长度100）" type="string"/>
            <form name="StartTime" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="EndTime" type="string" description="结束时间（yyyy-MM-dd HH:mm）"/>
            <form name="Duration" type="float" description="请假时长（单位为小时）"/>
            <form name="Reason" type="string" description="请假事由（最大长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="申请报销" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyPayment">
            <description>
                报销类别:
                差旅费 = 1,//火车票、飞机票、长途汽车票、订票费发票、住宿费发票、餐饮发票、打车票、地铁票、加油费、过路过桥费、租车费
                招待费 = 2,//餐饮业发票、住宿费发票、打车票、礼品发票、食品费
                车辆费 = 3,//加油费发票、过路过桥费发票、停车费发票、修理费发票（带明细）、保养费（带明细）、车辆保险
                培训费 = 4,//培训费发票、培训费收据（国家机关事业单位专用）、资料费发票。
                电话费 = 5,//电话费发票、手机费发票。
                网络费 = 6,//网络费发票、网络服务发票。
                咨询费 = 7,//咨询费发票（含咨询报告）。
                会议费 = 8,//会议费发票（会议决议）
                运输费 = 9,//运输业专用发票、装卸费发票。
                办公品 = 10,//办公用品发票（带明细）。
                劳保费 = 11,//劳保用品发票。
                邮寄费 = 12,//邮电业专用发票、服务业发票。
                宣传费 = 13,//广告费发票（合同）
                制作费 = 14,//增值税专用发票（带合同）
            </description>
            <form name="Type" description="报销类别" type="int"/>
            <form name="JinE" description="报销金额（单位为元）" type="decimal"/>
            <form name="Detail" type="string" description="报销明细（最大长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="申请物品" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyGoods">
            <form name="YongTu" description="物品领用用途（最大长度200）" type="string"/>
            <form name="Name" description="物品领用名称" type="float"/>
            <form name="Amounts" description="物品领用数量" type="int"/>
            <form name="Reason" type="string" description="物品领用事由（最大长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="通用申请" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=ApplyCommon">
            <form name="Content" description="申请内容（最大长度200）" type="string"/>
            <form name="Reason" type="string" description="申请原因（最大长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="请假详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetVacateById">
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//申请名称,
                DateTime Time ;//申请时间,
                int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                int Type ;// 请假类别:事假 = 1;病假 = 2;产假 = 3;婚假 = 4;丧假 = 5;年假 = 6;公假 = 7,
                DateTime StartTime ;//开始时间,
                DateTime EndTime ;//结束时间,
                int Duration ;//请假时长(小时),
                string Reason ;//请假事由,
                Images://图片["url","url",...];,
                Applicant://申请人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Approver://审批人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="休息详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetRestById">
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//申请名称,
                DateTime Time ;//申请时间,
                int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                DateTime StartTime ;//开始时间,
                DateTime EndTime ;//结束时间,
                int Duration ;//休息时长(小时),
                string Reason ;//休息事由,
                Images://图片["url","url",...];,
                Applicant://申请人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Approver://审批人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="出差详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetEvectionById">
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//申请名称,
                DateTime Time ;//申请时间,
                int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                string Address ;//出差地点,
                DateTime StartTime ;//开始时间,
                DateTime EndTime ;//结束时间,
                int Duration ;//出差时长(小时),
                string Reason ;//出差事由,
                Images://图片["url","url",...];,
                Applicant://申请人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Approver://审批人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="报销详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetPaymentById">
            <description>
                报销类别:
                差旅费 = 1,//火车票、飞机票、长途汽车票、订票费发票、住宿费发票、餐饮发票、打车票、地铁票、加油费、过路过桥费、租车费
                招待费 = 2,//餐饮业发票、住宿费发票、打车票、礼品发票、食品费
                车辆费 = 3,//加油费发票、过路过桥费发票、停车费发票、修理费发票（带明细）、保养费（带明细）、车辆保险
                培训费 = 4,//培训费发票、培训费收据（国家机关事业单位专用）、资料费发票。
                电话费 = 5,//电话费发票、手机费发票。
                网络费 = 6,//网络费发票、网络服务发票。
                咨询费 = 7,//咨询费发票（含咨询报告）。
                会议费 = 8,//会议费发票（会议决议）
                运输费 = 9,//运输业专用发票、装卸费发票。
                办公品 = 10,//办公用品发票（带明细）。
                劳保费 = 11,//劳保用品发票。
                邮寄费 = 12,//邮电业专用发票、服务业发票。
                宣传费 = 13,//广告费发票（合同）
                制作费 = 14,//增值税专用发票（带合同）
            </description>
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//申请名称,
                DateTime Time ;//申请时间,
                int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                int Type ;// 报销类别,
                decimal JinE ;//报销金额（单位为元）,
                string Detail ;//报销明细,
                Images://图片["url","url",...];,
                Applicant://申请人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Approver://审批人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="物品详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetGoodsById">
            <form name="YongTu" description="物品领用用途（最大长度200）" type="string"/>
            <form name="Name" description="物品领用名称" type="float"/>
            <form name="Amounts" description="物品领用数量" type="int"/>
            <form name="Reason" type="string" description="物品领用事由（最大长度500）"/>
            <form name="ApproverId" description="审批人Id" type="string"/>
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//申请名称,
                DateTime Time ;//申请时间,
                int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                string YongTu ;// 物品领用用途,
                string Name ;// 物品领用名称,
                int Amounts ;// 物品领用数量,
                string Reason ;//申请原因,
                Images://图片["url","url",...];,
                Applicant://申请人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Approver://审批人 {
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="通用详情" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetCommonById">
            <form name="Id" type="string" description="申请Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                    string Id ;//Id,
                    string Name ;//申请名称,
                    DateTime Time ;//申请时间,
                    int Status ;//审批状态:未审批 = 1;审批通过 = 2;审批不通过 = 3,
                    string Content ;//申请内容,
                    string Reason ;//申请原因,
                    Images://图片["url","url",...];,
                    Applicant://申请人 {
                        String Id;      //用户Id,
                        String Name;    //显示名称,
                        String Avatar;  //用户头像,
                    },
                    Approver://审批人 {
                        String Id;      //用户Id,
                        String Name;    //显示名称,
                        String Avatar;  //用户头像,
                    }
                }
                }
            </response>
        </api>
        <api name="审批通过" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=Approve">
            <description>
                申请类型:
                请假 = 1,
                休息 = 2,
                出差 = 3,
                报销 = 4,
                物品领用 = 5,
                通用申请 = 15
            </description>
            <form name="Id" type="string" description="申请Id"/>
            <form name="type" type="int" description="申请类型"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:""
                }
            </response>
        </api>
        <api name="不予通过" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=Disapprove">
            <description>
                申请类型:
                请假 = 1,
                休息 = 2,
                出差 = 3,
                报销 = 4,
                物品领用 = 5,
                通用申请 = 15
            </description>
            <form name="Id" type="string" description="申请Id"/>
            <form name="type" type="int" description="申请类型"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:""
                }
            </response>
        </api>
    </module>

    <module name="项目相关">
        <api name="进度列表" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetProjectProgressList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                int Progress ;//进度 0-100,
                Date Time ;//创建时间,

                string PrincipalIds;//项目负责人Ids（用于权限判断）,
                string QualityPrincipalIds;//质量创建人Ids（用于权限判断）,

                Initiator://创建人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                QualityPrincipal://质量负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="我的项目" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetMyProjectList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                int Progress ;//进度 0-100,
                Date Time ;//创建时间,

                string PrincipalIds;//项目负责人Ids（用于权限判断）,
                string QualityPrincipalIds;//质量创建人Ids（用于权限判断）,

                Initiator://创建人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                QualityPrincipal://质量负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="流程列表" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetProjectFlowListByProcessId">
            <form name="Id" description="工序Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                int Sort ;//排序,
                string GongXuId ;//工序Id,
                }...]
                }
            </response>
        </api>
        <api name="项目详情" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//名称,
                Date StartTime ;// 开始时间,
                string GongQi ;// 工期,
                decimal ContractValue ;// 合同金额（单位元）,
                string Address ;// 项目地址,
                int Floor ;// 楼层（单位层）,
                float Area ;// 面积（单位㎡）,
                string Remark ;// 备注,
                bool Complete ;// 项目是否完成（true标示完成，false表示未完成）,

                string PrincipalIds;//项目负责人Ids（用于权限判断）,
                string QualityPrincipalIds;//质量创建人Ids（用于权限判断）,

                string Avatar ;//头像,
                int StageCount ;//阶段,
                int ProcessCount ;//工序总数,
                int ProcessFinishedCount ;//已完成工序,
                int ProcessOvertimeCount ;//超期工序,

                Images://图片["url","url",...];,
                Initiator://创建人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                QualityPrincipal://质量负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="阶段详情" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetStageById">
            <form name="StageId" type="string" description="阶段Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//名称,
                Date EndDateTime ;// 预计结束时间,
                decimal DownPayment;// 阶段款项（元）
                bool Complete ;// 施工工序是否完成（true标示完成，false表示未完成）,
                }
                }
            </response>
        </api>
        <api name="工序详情" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetProcessById">
            <form name="ProcessId" type="string" description="工序Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//名称,
                int Sort ;// 流程排序,
                Date StartDateTime;开始时间,
                int Period;//工期单位(天),
                bool Complete ;// 施工工序是否完成（true标示完成，false表示未完成）,

                int FlowCount ;//流程总数
                int DrawingCount ;//图纸总量
                int DiaryCount ;//日志总量
                int QualityCount ;//质量总量

                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="项目内容" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetProjectContentById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                Date EndDateTime ;// 预计结束时间,
                decimal DownPayment;// 阶段款项（元）
                bool Complete ;// 施工工序是否完成（true标示完成，false表示未完成）,
                Process:[{
                string Id ;//Id,
                string Name ;//名称,
                int Sort ;// 流程排序,
                Date StartDateTime;开始时间,
                Date EndDateTime; 结束时间,
                int Period;//工期单位(天),
                bool Complete ;// 施工工序是否完成（true标示完成，false表示未完成）,

                int FlowCount ;//流程总数
                int DrawingCount ;//图纸总量
                int DiaryCount ;//日志总量
                int QualityCount ;//质量总量

                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                },...]
                },...]
                }
            </response>
        </api>
        <api name="创建项目" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=Create">
            <form name="Name" description="标题" type="string"/>
            <form name="StartTime" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="Address" description="项目位置" type="string"/>
            <form name="GongQi" description="工期" type="string"/>
            <form name="ContractValue" description="合同金额(元)" type="decimal"/>
            <form name="DirectorId" description="项目负责人" type="string"/>
            <form name="QualityDirectorId" description="质量负责人" type="string"/>
            <form name="Area" nullable="true" description="面积（单位㎡）" type="float"/>
            <form name="Floor" nullable="true" description="楼层（单位层）" type="int"/>
            <form name="Remark" nullable="true" description="备注" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="创建阶段" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=CreateStage">
            <form name="Name" description="标题" type="string"/>
            <form name="EndDateTime" type="string" description="预计结束时间（yyyy-MM-dd HH:mm）"/>
            <form name="DownPayment" description="阶段款项（元）" type="decimal"/>
            <form name="ProjectId" description="项目Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="创建工序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=CreateProcess">
            <form name="Name" description="标题" type="string"/>
            <form name="StartDateTime" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="Period" description="工期（单位为天）" type="float"/>
            <form name="Flows" description="流程名称" type="string" example="流程1,流程2"/>
            <form name="ProjectId" description="项目Id" type="string"/>
            <form name="ConstructionStageId" description="阶段Id" type="string"/>
            <form name="DirectorId" nullable="true" description="负责人Id" type="float"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="创建流程" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=CreateFlow">
            <form name="Name" description="标题" type="string"/>
            <form name="GongXuId" description="工序Id" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="更新项目" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateProject">
            <form name="Id" type="string" description="指定Id"/>
            <form name="Name" nullable="true" description="标题" type="string"/>
            <form name="StartTime" nullable="true" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="Address" nullable="true" description="项目位置" type="string"/>
            <form name="GongQi" nullable="true" description="工期" type="string"/>
            <form name="ContractValue" nullable="true" description="合同金额(元)" type="decimal"/>
            <form name="Area" nullable="true" description="面积（单位㎡）" type="float"/>
            <form name="Floor" nullable="true" description="楼层（单位层）" type="int"/>
            <form name="DirectorId" nullable="true" description="项目负责人" type="string"/>
            <form name="QualityDirectorId" nullable="true" description="质量负责人" type="string"/>
            <form name="Floor" nullable="true" description="楼层（单位层）" type="int"/>
            <form name="Remark" nullable="true" description="备注" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="更新阶段" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateStage">
            <form name="Id" type="string" description="指定Id"/>
            <form name="Name" description="标题" type="string"/>
            <form name="DownPayment" description="阶段款项（元）" type="decimal"/>
            <form name="EndDateTime" type="string" description="预计结束时间（yyyy-MM-dd HH:mm）"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="更新工序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateProcess">
            <form name="Id" type="string" description="指定Id"/>
            <form name="Name" nullable="true" description="标题" type="string"/>
            <form name="StartDateTime" nullable="true" type="string" description="开始时间（yyyy-MM-dd HH:mm）"/>
            <form name="Period" nullable="true" description="工期（单位为天）" type="float"/>
            <form name="DirectorId" nullable="true" description="负责人Id" type="float"/>
            <form name="Flows" nullable="true" description="流程名称" type="string" example="流程1,流程2"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="流程排序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateFlowSort">
            <form name="Ids" description="指定流程Id（可多个）" type="string" example="001,002,003 或者 001"/>
            <form name="Sorts" description="排序索引（可多个）" type="string" example="0,1,2,3 或者 0"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="工序排序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateProcessSort">
            <form name="Ids" description="指定工序Id（可多个）" type="string" example="001,002,003 或者 001"/>
            <form name="Sorts" description="排序索引（可多个）" type="string" example="0,1,2,3 或者 0"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="阶段排序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=UpdateStageSort">
            <form name="Ids" description="指定阶段Id（可多个）" type="string" example="001,002,003 或者 001"/>
            <form name="Sorts" description="排序索引（可多个）" type="string" example="0,1,2,3 或者 0"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="删除流程" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=DeleteFlow">
            <form name="Ids" type="string" description="流程Id（可多个）" example="001,002 或者 001"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="删除工序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=DeleteProcess">
            <form name="Ids" type="string" description="工序Id（可多个）" example="001,002 或者 001"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="删除阶段" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=DeleteStage">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="删除项目" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=DeleteProject">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="完成工序" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=FinishProcess">
            <form name="ProcessId" type="string" description="工序Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="成员列表" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetMemberListById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                String Avatar;  //头像,
                int Type ;//0 普通成员 1 质量负责人 2 项目负责人 3 项目创建人
                }...]
                }
            </response>
        </api>
        <api name="添加成员" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=AddMembers">
            <form name="Id"  type="string" description="项目Id"/>
            <form name="Ids"  type="string" description="用户Id" example="001,002,003"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://,
                }
            </response>
        </api>
        <api name="移除成员" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=DelMembers">
            <form name="Id"  type="string" description="项目Id"/>
            <form name="Ids"  type="string" description="用户Id" example="001,002,003"/>
            <form name="Types"  type="string" description="Ids对应角色：0 普通成员 1 质量负责人 2 项目负责人" example="0,1,2"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://,
                }
            </response>
        </api>
        <api name="成员角色" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=SetMemberRole">
            <form name="Id"  type="string" description="项目Id"/>
            <form name="UserId"  type="string" description="用户Id"/>
            <form name="Type"  type="string" description="0 普通成员 1 质量负责人 2 项目负责人"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://,
                }
            </response>
        </api>
    </module>

    <module name="用户消息">
        <api name="消息轮询" requestMethod="POST" path="/ios-r.ashx?$c=UserMessage&amp;$m=GetNewMyMessage">
            <description>
                访问的接口需要需要把超时时间设长大于必须1分钟。
                本接口需要不断的重复调用，来获取实时消息数据。
                LastTime 第一次使用0，调用结束之后，LastTime取值为列表中最大的LongTime,下一次继续调用
            </description>
            <form name="LastTime" type="long" description="看接口说明"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                bool IsReaded;   //是否阅读,
                Date Time;       //时间,
                long LongTime;   //long格式的时间:用于再次请求本接口使用的：LastTime,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="接收列表" requestMethod="POST" path="ios.ashx?$c=UserMessage&amp;$m=GetMyRecvedMessageList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                bool IsReaded;   //是否阅读,
                Date Time;       //时间,
                Initiator://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="发送列表" requestMethod="POST" path="ios.ashx?$c=UserMessage&amp;$m=GetMySendedMessageList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                Date Time;       //时间,
                Recipient://接收人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="消息详情" requestMethod="POST" path="ios.ashx?$c=UserMessage&amp;$m=GetById">
            <form name="Id" type="string" description="Id"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                String Id;       //Id,
                String Title;    //公告标题,
                String Content;  //公告内容,
                bool IsReaded;   //是否阅读,
                Date Time;       //发布时间,
                Images://图片["url","url",...],
                Initiator://消息发起人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Recipient://接收人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="阅读消息" requestMethod="POST" path="ios.ashx?$c=UserMessage&amp;$m=ReadById">
            <form name="Id" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                }
            </response>
        </api>
        <api name="发送信息" requestMethod="POST" path="ios.ashx?$c=UserMessage&amp;$m=Send">
            <form name="Title" description="公告标题" type="string"/>
            <form name="Content" description="公告内容" type="string"/>
            <form name="TargetIds" description="接收对象Id" example="001,002,003" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
    </module>

    <module name="消息通知">
        <api name="消息轮询" requestMethod="POST" path="/ios-r.ashx?$c=Message&amp;$m=GetMyNewMessage">
            <description>
                访问的接口需要需要把超时时间设长大于必须1分钟。
                本接口需要不断的重复调用，来获取实时消息数据。
                LastTime 第一次使用0，调用结束之后，LastTime取值为列表中最大的LongTime,下一次继续调用；
                注意处理消息数据：当时【Type=团队申请结果】时候，需要获取结果详情，来判断是否通过申请，来实时改变团队状态。
            </description>
            <form name="LastTime" type="long" description="看接口说明"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                String TargetId; //与消息相关的Id（可空）,
                int Type;        //消息类型（用户审核 = 1,团队申请 = 2,团队邀请 = 3, 团队申请结果 = 4, 团队邀请结果 = 5）,
                bool IsReaded;   //是否阅读,
                Date Time;       //时间,
                long LongTime;       //long格式的时间:用于再次请求本接口使用的：LastTime,
                Initiator://发布人（可空）{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="消息数量" requestMethod="POST" path="ios.ashx?$c=Message&amp;$m=GetMyNewMessageCount">
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:0//数量
                }
            </response>
        </api>
        <api name="消息列表" requestMethod="POST" path="ios.ashx?$c=Message&amp;$m=GetMyMessageList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;       //Id,
                String Title;    //标题,
                String Content;  //内容,
                String TargetId; //与消息相关的Id（可空）,
                int Type;        //消息类型（用户审核 = 1,团队申请 = 2,团队邀请 = 3, 团队申请结果 = 4, 团队邀请结果 = 5）,
                bool IsReaded;   //是否阅读,
                Date Time;       //时间,
                Initiator://发布人（可空）{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="阅读消息" requestMethod="POST" path="ios.ashx?$c=Message&amp;$m=ReadById">
            <form name="Id" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                }
            </response>
        </api>
    </module>

    <module name="团队相关">
        <api name="创建团队" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Create">
            <form name="Name" description="名称" type="string"/>
            <form name="LogoUrl" description="头像" type="string"/>
            <form name="OrgName" description="公司名称" type="string"/>
            <form name="Address" description="公司地址" type="string"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="更新团队" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Update">
            <form name="OrgId" type="string" description="机构Id"/>
            <form name="Name" description="名称" type="string"/>
            <form name="LogoUrl" description="头像" type="string"/>
            <form name="OrgName" description="公司名称" type="string"/>
            <form name="Address" description="公司地址" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="搜索团队" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Search">
            <form name="Key" type="string" description="名称关键字"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                string Avatar ;//头像,
                }...],
                }
            </response>
        </api>
        <api name="团队信息" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=GetById">
            <form name="OrgId" description="部门Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//名称,
                string Avatar ;//头像,
                string OrgName ;//公司名称,
                string Address ;//公司地址,
                string ManagerIds ;//管理员Ids，用于判断权限,
                int MemberCount;//成员数量,
                Creater://发布人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="退出团队" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Quit">
            <form name="OrgId" type="string" description="团队ID"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="加入团队" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Join">
            <form name="OrgId" type="string" description="团队ID"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="邀请加入" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=Invite">
            <form name="UserIds" type="string" description="用户ID" example="001,002"/>
            <form name="DepartmentId" type="string" description="部门Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="申请处理" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=JoinAccept">
            <form name="ApplyId" type="string" description="申请ID"/>
            <form name="Accept" type="boolean" description="是否通过"/>
            <form name="DepartmentId" nullable="true" type="string" description="部门Id（通过时必须）"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="邀请处理" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=InviteAccept">
            <form name="InviteId" type="string" description="邀请ID"/>
            <form name="Accept" type="boolean" description="是否通过"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="申请详情" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=GetJoinById">
            <form name="ApplyId" description="申请Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                int Status ;//状态 （0:未处理 1:通过 2:拒绝）,
                date ApplyTime ;//申请时间,
                Applyer://申请人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                String Contact; //联系方式
                },
                Organization://团队机构{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }
                }
            </response>
        </api>
        <api name="邀请详情" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=GetInviteById">
            <form name="InviteId" description="邀请Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                int Status ;//状态 （0:未处理 1:通过 2:拒绝）,
                date ApplyTime ;//申请时间,
                Inviter://邀请人{
                String Id;      //Id,
                String Name;    //名称,
                String Avatar;  //头像,
                String Contact; //联系方式
                },
                Organization://团队机构{
                String Id;      //Id,
                String Name;    //名称,
                String Avatar;  //头像,
                },
                Department://分配部门{
                String Id;      //Id,
                String Name;    //名称,
                String Avatar;  //头像,
                }
                }
                }
            </response>
        </api>
        <api name="搜索员工" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=SearchUser">
            <form name="Key" type="string" description="关键字"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                string Avatar ;//头像,
                }...],
                }
            </response>
        </api>
        <api name="移动员工" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=MoveUsers">
            <form name="UserIds" type="string" description="人员Id" example="001,002,003"/>
            <form name="DepartmatnId" type="string" description="部门Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="移除员工" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=RemoveUsers">
            <form name="UserIds" type="string" description="人员Id" example="001,002,003"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="权限列表" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=GetManagerListById">
            <description>获取指定团队管理员列表</description>
            <form name="OrgId" type="string" description="团队ID"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }...]
                }
            </response>
        </api>
        <api name="添加管理" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=AddManagers">
            <form name="UserIds" type="string" description="人员Id" example="001,002,003"/>
            <form name="OrgId" type="string" description="团队ID"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="移除管理" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=DelManagers">
            <form name="OrgId" type="string" description="团队ID"/>
            <form name="UserIds" type="string" description="人员Id" example="001,002,003"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
        <api name="获取考勤" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=GetAttendanceSetById">
            <form name="OrgId" type="string" description="团队ID"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id;//Id,
                string AMStart;//上班时间,
                string PMEnd;//下班时间,
                string Weekday;//周期（0星期天 ... 6 星期六）,
                string Address;//打卡位置,
                string SignScope,//签到范围 单位米
                Location://定位信息（可能为null）{
                double latitude,
                double longitude
                }
                },
                }
            </response>
        </api>
        <api name="修改考勤" requestMethod="POST" path="ios.ashx?$c=Organization&amp;$m=SetAttendanceSetById">
            <form name="OrgId" type="string" description="团队ID"/>
            <form name="AMStart" nullable="true" type="string" description="上班时间"/>
            <form name="PMEnd" nullable="true" type="string" description="下班时间"/>
            <form name="Weekday" nullable="true" type="string" description="周期"/>
            <form name="Address" nullable="true" type="string" description="打卡位置"/>
            <form name="SignScope" nullable="true" type="int" description="签到范围 单位米"/>
            <form name="latitude" nullable="true" type="double" description="位置信息"/>
            <form name="longitude" nullable="true" type="double" description="位置信息"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:,
                }
            </response>
        </api>
    </module>

    <module name="部门相关">
        <api name="部门列表" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=GetBranchListById">
            <form name="OrgId" nullable="true" description="机构Id" type="string"/>
            <form name="DepartmentId" nullable="true" description="上级部门Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                string OrgId ;//机构Id,
                string ParentId ;//父级Id,
                }...]
                }
            </response>
        </api>
        <api name="部门树形" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=GetDepartmentTree">
            <form name="OrgId" nullable="true" description="机构Id" type="string"/>
            <form name="DepartmentId" nullable="true" description="上级部门Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                string OrgId ;//机构Id,
                string ParentId ;//父级Id,
                int MemberCount ;//成员数量,
                Items:[];//树形嵌套,
                }...]
                }
            </response>
        </api>
        <api name="创建部门" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=Create">
            <form name="Name" description="名称" type="string"/>
            <form name="OrgId" description="机构Id" type="string"/>
            <form name="ParentId" nullable="true" description="上级部门Id（顶层部门可不传）" type="string"/>
            <form name="ManagerId" nullable="true" description="管理人Id" type="string"/>
            <form name="Branches" nullable="true" description="子部门名称（可多个）" type="string" example="财务部,行政部"/>
            <form name="Id" nullable="true" type="string" description="指定Id"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data://新的Id,
                }
            </response>
        </api>
        <api name="更新部门" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=Update">
            <form name="DepartmentId" description="部门Id" type="string"/>
            <form name="Name" description="名称" type="string"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                int Sort ;//排序,
                string GongXuId ;//工序Id,
                }...]
                }
            </response>
        </api>
        <api name="获取部门" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=GetById">
            <form name="DepartmentId" description="部门Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:{
                string Id ;//Id,
                string Name ;//名称,
                }
                }
            </response>
        </api>
        <api name="删除部门" requestMethod="POST" path="ios.ashx?$c=Department&amp;$m=Delete">
            <form name="DepartmentId" description="部门Id" type="string"/>
            <response contentType="Json">
                {
                success:0 //true 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:
                }
            </response>
        </api>
    </module>

    <module name="工人版本">
        <api name="我的项目" requestMethod="POST" path="ios.ashx?$c=Project&amp;$m=GetMyProjectList">
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//名称,
                int Progress ;//进度 0-100,
                Date Time ;//创建时间,

                string PrincipalIds;//项目负责人Ids（用于权限判断）,
                string QualityPrincipalIds;//质量创建人Ids（用于权限判断）,

                Initiator://创建人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                Principal://负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                },
                QualityPrincipal://质量负责人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
        <api name="请假列表" requestMethod="POST" path="ios.ashx?$c=Approval&amp;$m=GetMyVacateList">
            <description>
                我发出的申请
            </description>
            <param name="pageSize" description="分页大小" type="int"/>
            <param name="page" description="页数" type="int"/>
            <response contentType="Json">
                {
                success:0 //0 表示成功 否则 data 为null msg 可以获取错误信息,
                msg:"",
                data:[{
                string Id ;//Id,
                string Name ;//申请名称,
                string Content ;//申请内容,
                DateTime Time ;//申请时间,
                int ApprovalType;//类型 请假 = 1;休息 = 2;出差 = 3;报销 = 4;物品领用 = 5;通用申请 = 15,
                int Status ;//状态 未审批 = 1；审批通过 = 2；审批不通过 = 3,
                Approver://审批人{
                String Id;      //用户Id,
                String Name;    //显示名称,
                String Avatar;  //用户头像,
                }
                }...]
                }
            </response>
        </api>
    </module>

</service>