package ${packageName}.mapper.bean;

import org.mapstruct.Mapper;

import java.util.List;


/**
 * Bean转换 mapper 接口
 * @author ${author}
 * @since ${now?string("yyyy-MM-dd zzzz")}
 */
@Mapper(componentModel = "spring")
public interface BeanMapper {

}
