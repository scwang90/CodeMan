<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${packageName}.mapper.common.${className}Mapper">
  <resultMap id="${table.name}" type="${packageName}.model.db.${className}">
    <!--<#list table.columns as column>-->
    <!--${column.remark}<#if table.idColumn.nameSQL == column.nameSQL>-->
    <id column="${column.nameSQL}" jdbcType="${column.typeJdbc}" property="${column.fieldName}" />
    <!--<#else >-->
    <result column="${column.nameSQL}" jdbcType="${column.typeJdbc}" property="${column.fieldName}" />
    <!--</#if>-->
    <!--</#list>-->
  </resultMap>
</mapper>

